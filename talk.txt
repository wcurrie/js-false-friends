Revisit:
 => functions in ES6
 simpler this binding in Typescript and Coffeescript
 ES6 let statement

Add command+enter to execute script demos

Add 'js is the only language people can program without reading a book quote'


{}
* variables are global without 'var'
* hoisting

closures
* for loop to build a set of buttons

for in loop can return unwanted members

where are my classes?

== vs === 
* type coercion

only one thread
* show for loop locking up the UI?
